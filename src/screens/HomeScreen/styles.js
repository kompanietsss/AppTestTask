import {StyleSheet} from 'react-native';
import {h} from '../../helpers/misc';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  postsContainer: {paddingBottom: (2 * h) / 100},
});
